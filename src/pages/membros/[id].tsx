import { default as MembrosLayout } from '~/layouts/membros';
import { getProps as getProps } from '~/layouts/membros';

export default MembrosLayout;

export const getServerSideProps = getProps;
