import { default as CheckoutLayout } from '~/layouts/checkout';
import { getProps as getProps } from '~/layouts/checkout';

export default CheckoutLayout;

export const getServerSideProps = getProps;
